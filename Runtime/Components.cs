namespace Fury.ECS
{
    internal struct Components
    {
        public readonly int ComponentSize;
        public Components(int componentSize)
        {
            ComponentSize = componentSize;
        }
    }
}
